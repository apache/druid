type: "operatorValidation"

sql: |
  SELECT
    dim2,
    LAG(dim2,1) OVER ()
  FROM foo
  WHERE dim2 is null or length(dim2)>0
  GROUP BY dim2

expectedOperators:
  - type: "naivePartition"
    partitionColumns: []
  - type: "window"
    processor:
      type: "offset"
      inputColumn: "d0"
      outputColumn: "w0"
      offset: -1
expectedResults:
  - [null,null]
  - ["a",null]
  - ["abc","a"]
