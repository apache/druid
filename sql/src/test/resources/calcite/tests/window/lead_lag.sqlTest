type: "operatorValidation"

sql: |
  SELECT
  	dim1,
    LEAD(dim1) OVER () cc
  FROM foo
  GROUP BY dim1

expectedOperators:
  - type: "naiveSort"
    columns:
    - column: dim2
      direction: ASC
    - column: dim1
      direction: ASC
  - type: "naivePartition"
    partitionColumns: ["dim2"]
  - type: "window"
    processor:
      type: "framedAgg"
      frame: { peerType: "ROWS", lowUnbounded: true, lowOffset: 0, uppUnbounded: false, uppOffset: 0 }
      aggregations:
        - { type: "doubleSum", name: "w0", fieldName: "m1" }

expectedResults:
  - [ "", 1.0 ]
#  SELECT 
#  col3 , #
#  LAG(col3) OVER ( PARTITION BY col3 ORDER BY col1 ) LAG_col3 
#  FROM "fewRowsAllData.parquet"
  