!set useApproximateCountDistinct false
!use druidtest://?componentSupplier=AllDruidEnginesComponentSupplier&datasets=sql/src/test/quidem/qatests/qaWin
!set outputformat mysql
#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A3_first_last_W19_G1_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip ASC,
                                                        server_ip,
                                                        length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                               timezone,
                                                                               time_floor(__time, 'PT12H') ROWS BETWEEN -1 PRECEDING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A3_first_last_W19_G2_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, server_ip, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN -1 PRECEDING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, server_ip, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN -1 PRECEDING AND UNBOUNDED FOLLOWING) last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A3_first_last_W19_G2_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip ASC,
                                                           server_ip,
                                                           length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                                  timezone,
                                                                                  time_floor(__time, 'PT12H') ROWS BETWEEN -1 PRECEDING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A3_first_last_W20_G1_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, server_ip, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN -1 PRECEDING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, server_ip, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN -1 PRECEDING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A3_first_last_W20_G1_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip DESC,
                                                        server_ip,
                                                        length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                timezone,
                                                                                time_floor(__time, 'PT12H') ROWS BETWEEN -1 PRECEDING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A3_first_last_W20_G2_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, server_ip, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN -1 PRECEDING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, server_ip, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN -1 PRECEDING AND UNBOUNDED FOLLOWING) last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A3_first_last_W20_G2_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip DESC,
                                                           server_ip,
                                                           length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                   timezone,
                                                                                   time_floor(__time, 'PT12H') ROWS BETWEEN -1 PRECEDING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A3_first_last_W21_G1_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip ROWS BETWEEN -1 PRECEDING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip ROWS BETWEEN -1 PRECEDING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A3_first_last_W21_G1_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip ROWS BETWEEN -1 PRECEDING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A3_first_last_W21_G2_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip ROWS BETWEEN -1 PRECEDING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip ROWS BETWEEN -1 PRECEDING AND UNBOUNDED FOLLOWING) last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A3_first_last_W21_G2_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip ROWS BETWEEN -1 PRECEDING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A3_first_last_W22_G1_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (ROWS BETWEEN -1 PRECEDING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (ROWS BETWEEN -1 PRECEDING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A3_first_last_W22_G1_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (ROWS BETWEEN -1 PRECEDING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A3_first_last_W22_G2_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (ROWS BETWEEN -1 PRECEDING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (ROWS BETWEEN -1 PRECEDING AND UNBOUNDED FOLLOWING) last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A3_first_last_W22_G2_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (ROWS BETWEEN -1 PRECEDING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W1_G1_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W1_G1_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip ASC,
                                                        timezone,
                                                        time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W1_G2_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W1_G2_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip ASC,
                                                           timezone,
                                                           time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W2_G1_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W2_G1_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip DESC,
                                                        timezone,
                                                        time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W2_G2_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W2_G2_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip DESC,
                                                           timezone,
                                                           time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W3_G1_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY client_ip, server_ip ASC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY client_ip, server_ip ASC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W3_G1_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY client_ip,
                                                        server_ip ASC,
                                                        timezone,
                                                        time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W3_G2_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY client_ip, server_ip ASC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY client_ip, server_ip ASC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W3_G2_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY client_ip,
                                                           server_ip ASC,
                                                           timezone,
                                                           time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W4_G1_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY client_ip, server_ip DESC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY client_ip, server_ip DESC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W4_G1_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY client_ip,
                                                        server_ip DESC,
                                                        timezone,
                                                        time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W4_G2_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY client_ip, server_ip DESC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY client_ip, server_ip DESC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W4_G2_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY client_ip,
                                                           server_ip DESC,
                                                           timezone,
                                                           time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W5_G1_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, length(server_ip) ASC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, length(server_ip) ASC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W5_G1_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip,
                                                        length(server_ip) ASC, timezone,
                                                                               time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W5_G2_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, length(server_ip) ASC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, length(server_ip) ASC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W5_G2_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip,
                                                           length(server_ip) ASC, timezone,
                                                                                  time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W6_G1_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, length(server_ip) DESC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, length(server_ip) DESC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W6_G1_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip,
                                                        length(server_ip) DESC, timezone,
                                                                                time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W6_G2_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, length(server_ip) DESC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, length(server_ip) DESC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W6_G2_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip,
                                                           length(server_ip) DESC, timezone,
                                                                                   time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W7_G1_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY client_ip, server_ip, length(server_ip) ASC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY client_ip, server_ip, length(server_ip) ASC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W7_G1_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY client_ip,
                                                        server_ip,
                                                        length(server_ip) ASC, timezone,
                                                                               time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W7_G2_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY client_ip, server_ip, length(server_ip) ASC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY client_ip, server_ip, length(server_ip) ASC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W7_G2_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY client_ip,
                                                           server_ip,
                                                           length(server_ip) ASC, timezone,
                                                                                  time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W8_G1_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY client_ip, server_ip, length(server_ip) DESC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY client_ip, server_ip, length(server_ip) DESC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W8_G1_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY client_ip,
                                                        server_ip,
                                                        length(server_ip) DESC, timezone,
                                                                                time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W8_G2_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY client_ip, server_ip, length(server_ip) DESC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY client_ip, server_ip, length(server_ip) DESC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W8_G2_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY client_ip,
                                                           server_ip,
                                                           length(server_ip) DESC, timezone,
                                                                                   time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W9_G1_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W9_G1_Q2
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM test_win
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip,
                                                        cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                        timezone,
                                                        time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TESTCASE: test_order_by TEST_ID: A4_first_last_W9_G2_Q1
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone, time_floor(__time, 'PT12H') ROWS BETWEEN UNBOUNDED PRECEDING AND -1 FOLLOWING) last_val
FROM test_win
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

