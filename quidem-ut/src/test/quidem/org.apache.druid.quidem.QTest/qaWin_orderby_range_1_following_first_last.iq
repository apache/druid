!set useApproximateCountDistinct false
!use druidtest:///?componentSupplier=KttmNestedComponentSupplier
!set outputformat mysql
SELECT count(*) cnt
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
+-----+
| cnt |
+-----+
| 768 |
+-----+
(1 row)

!ok

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W1_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, timezone RANGE 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W1_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip ASC,
                                                        timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W1_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, timezone RANGE 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W1_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip ASC,
                                                           timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W2_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, timezone RANGE 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W2_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip DESC,
                                                        timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W2_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, timezone RANGE 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W2_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip DESC,
                                                           timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W3_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, timezone RANGE 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W3_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip ASC,
                                                        timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W3_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, timezone RANGE 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W3_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip ASC,
                                                           timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W4_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, timezone RANGE 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W4_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip DESC,
                                                        timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W4_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, timezone RANGE 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W4_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip DESC,
                                                           timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W5_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) ASC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) ASC, timezone RANGE 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W5_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY length(server_ip) ASC, timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W5_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) ASC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) ASC, timezone RANGE 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W5_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY length(server_ip) ASC, timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W6_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) DESC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) DESC, timezone RANGE 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W6_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY length(server_ip) DESC, timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W6_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) DESC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) DESC, timezone RANGE 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W6_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY length(server_ip) DESC, timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W7_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) ASC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) ASC, timezone RANGE 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W7_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY length(server_ip) ASC, timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W7_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) ASC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) ASC, timezone RANGE 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W7_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY length(server_ip) ASC, timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W8_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) DESC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) DESC, timezone RANGE 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W8_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY length(server_ip) DESC, timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W8_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) DESC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) DESC, timezone RANGE 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W8_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY length(server_ip) DESC, timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W9_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W9_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                        timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W9_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W9_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                           timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W10_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W10_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                        timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W10_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W10_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                           timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W11_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W11_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                        timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W11_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W11_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                           timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W12_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W12_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                        timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W12_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W12_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                           timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W13_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W13_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') ASC, timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W13_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W13_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') ASC, timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W14_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W14_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') DESC, timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W14_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W14_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') DESC, timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W15_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W15_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') ASC, timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W15_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W15_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') ASC, timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W16_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W16_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') DESC, timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W16_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W16_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') DESC, timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W17_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W17_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip ASC,
                                                        length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                               timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W17_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W17_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip ASC,
                                                           length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                                  timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W18_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W18_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip DESC,
                                                        length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W18_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W18_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip DESC,
                                                           length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                   timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W19_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W19_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip ASC,
                                                        length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                               timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W19_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W19_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip ASC,
                                                           length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                                  timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W20_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W20_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip DESC,
                                                        length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W20_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W20_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip DESC,
                                                           length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                   timezone RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W21_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip RANGE 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W21_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W21_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip RANGE 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W21_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W22_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (RANGE 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W22_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W22_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (RANGE 1 FOLLOWING) first_val,
       last_value(timezone) OVER (RANGE 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A1_first_last_W22_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (RANGE 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W1_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W1_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W1_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W1_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W2_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W2_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W2_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W2_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W3_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W3_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W3_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W3_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W4_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W4_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W4_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W4_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W5_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W5_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W5_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W5_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W6_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W6_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W6_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W6_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W7_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W7_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W7_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W7_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W8_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W8_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W8_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W8_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W9_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W9_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W9_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W9_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W10_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W10_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W10_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W10_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W11_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W11_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W11_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W11_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W12_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W12_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W12_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W12_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W13_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W13_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W13_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W13_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W14_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W14_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W14_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W14_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W15_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W15_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W15_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W15_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W16_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W16_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W16_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W16_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W17_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W17_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip ASC,
                                                        length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                               timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W17_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W17_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip ASC,
                                                           length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                                  timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W18_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W18_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip DESC,
                                                        length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W18_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W18_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip DESC,
                                                           length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                   timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W19_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W19_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip ASC,
                                                        length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                               timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W19_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W19_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip ASC,
                                                           length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                                  timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W20_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W20_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip DESC,
                                                        length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W20_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W20_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip DESC,
                                                           length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                   timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W21_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W21_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W21_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W21_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W22_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W22_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W22_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) first_val,
       last_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A2_first_last_W22_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W1_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W1_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W1_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W1_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W2_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W2_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W2_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W2_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W3_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W3_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W3_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W3_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W4_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W4_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W4_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W4_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W5_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W5_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W5_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W5_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W6_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W6_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W6_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W6_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W7_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W7_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W7_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W7_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W8_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W8_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W8_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W8_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W9_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W9_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W9_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W9_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W10_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W10_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W10_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W10_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W11_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W11_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W11_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W11_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W12_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W12_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W12_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W12_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W13_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W13_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W13_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W13_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W14_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W14_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W14_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W14_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W15_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W15_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W15_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W15_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W16_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W16_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W16_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W16_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W17_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W17_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip ASC,
                                                        length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                               timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W17_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W17_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip ASC,
                                                           length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                                  timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W18_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W18_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip DESC,
                                                        length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W18_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W18_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip DESC,
                                                           length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                   timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W19_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W19_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip ASC,
                                                        length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                               timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W19_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W19_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip ASC,
                                                           length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                                  timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W20_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W20_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip DESC,
                                                        length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W20_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W20_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip DESC,
                                                           length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                   timezone RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W21_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W21_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W21_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W21_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W22_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W22_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W22_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) first_val,
       last_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A3_first_last_W22_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (RANGE BETWEEN 1 FOLLOWING AND 1 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W1_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W1_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W1_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W1_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W2_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W2_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W2_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W2_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W3_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W3_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W3_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W3_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W4_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W4_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W4_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W4_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W5_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W5_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W5_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W5_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W6_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W6_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W6_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W6_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W7_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W7_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W7_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W7_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W8_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W8_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W8_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W8_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W9_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W9_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W9_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W9_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W10_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W10_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W10_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W10_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W11_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W11_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W11_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W11_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W12_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W12_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W12_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W12_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W13_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W13_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W13_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W13_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W14_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W14_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W14_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W14_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W15_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W15_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W15_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W15_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W16_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W16_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W16_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W16_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W17_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W17_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip ASC,
                                                        length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                               timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W17_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W17_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip ASC,
                                                           length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                                  timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W18_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W18_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip DESC,
                                                        length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W18_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W18_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip DESC,
                                                           length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                   timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W19_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W19_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip ASC,
                                                        length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                               timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W19_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W19_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip ASC,
                                                           length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                                  timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W20_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W20_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip DESC,
                                                        length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W20_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W20_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip DESC,
                                                           length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                   timezone RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W21_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W21_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W21_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W21_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W22_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W22_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W22_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) first_val,
       last_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A4_first_last_W22_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (RANGE BETWEEN 1 FOLLOWING AND 0 PRECEDING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W1_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W1_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W1_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W1_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W2_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W2_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W2_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W2_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W3_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W3_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W3_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W3_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W4_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W4_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W4_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W4_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W5_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W5_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W5_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W5_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W6_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W6_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W6_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W6_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W7_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W7_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W7_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W7_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W8_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W8_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W8_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W8_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W9_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W9_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W9_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W9_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W10_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W10_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W10_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W10_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W11_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W11_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W11_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W11_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W12_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W12_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W12_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W12_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W13_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W13_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W13_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W13_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W14_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W14_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W14_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W14_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W15_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W15_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W15_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W15_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W16_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W16_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W16_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W16_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W17_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W17_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip ASC,
                                                        length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                               timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W17_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W17_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip ASC,
                                                           length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                                  timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W18_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W18_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip DESC,
                                                        length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W18_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W18_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip DESC,
                                                           length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                   timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W19_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W19_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip ASC,
                                                        length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                               timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W19_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W19_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip ASC,
                                                           length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                                  timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W20_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W20_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip DESC,
                                                        length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W20_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W20_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip DESC,
                                                           length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                   timezone RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W21_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W21_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W21_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W21_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W22_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W22_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W22_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) first_val,
       last_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A5_first_last_W22_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (RANGE BETWEEN 1 FOLLOWING AND CURRENT ROW);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W1_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W1_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W1_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W1_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W2_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W2_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W2_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W2_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W3_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W3_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W3_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W3_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W4_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W4_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W4_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W4_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W5_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W5_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W5_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W5_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W6_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W6_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W6_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W6_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W7_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W7_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W7_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W7_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W8_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W8_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W8_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W8_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W9_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W9_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W9_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W9_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W10_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W10_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W10_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W10_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W11_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W11_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W11_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W11_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W12_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W12_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W12_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W12_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W13_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W13_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W13_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W13_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W14_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W14_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W14_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W14_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W15_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W15_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W15_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W15_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W16_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W16_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W16_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W16_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W17_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W17_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip ASC,
                                                        length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                               timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W17_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W17_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip ASC,
                                                           length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                                  timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W18_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W18_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip DESC,
                                                        length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W18_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W18_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip DESC,
                                                           length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                   timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W19_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W19_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip ASC,
                                                        length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                               timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W19_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W19_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip ASC,
                                                           length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                                  timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W20_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W20_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip DESC,
                                                        length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W20_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W20_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip DESC,
                                                           length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                   timezone RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W21_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W21_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W21_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W21_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W22_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W22_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W22_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) first_val,
       last_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A6_first_last_W22_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (RANGE BETWEEN 1 FOLLOWING AND 0 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W1_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W1_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W1_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W1_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W2_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W2_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W2_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W2_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W3_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W3_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W3_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W3_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W4_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W4_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W4_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W4_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W5_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W5_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W5_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W5_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W6_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W6_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W6_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W6_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W7_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W7_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W7_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W7_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W8_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W8_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W8_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W8_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W9_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W9_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W9_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W9_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W10_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W10_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W10_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W10_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W11_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W11_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W11_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W11_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W12_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W12_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W12_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W12_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W13_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W13_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W13_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W13_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W14_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W14_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W14_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W14_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W15_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W15_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W15_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W15_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W16_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W16_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W16_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W16_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W17_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W17_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip ASC,
                                                        length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                               timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W17_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W17_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip ASC,
                                                           length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                                  timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W18_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W18_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip DESC,
                                                        length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W18_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W18_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip DESC,
                                                           length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                   timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W19_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W19_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip ASC,
                                                        length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                               timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W19_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W19_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip ASC,
                                                           length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                                  timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W20_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W20_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip DESC,
                                                        length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W20_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W20_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip DESC,
                                                           length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                   timezone RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W21_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W21_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W21_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W21_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W22_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W22_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W22_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) first_val,
       last_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A7_first_last_W22_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (RANGE BETWEEN 1 FOLLOWING AND 1 FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W1_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W1_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W1_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W1_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W2_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W2_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W2_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W2_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W3_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W3_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W3_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W3_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W4_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W4_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W4_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W4_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W5_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W5_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W5_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W5_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W6_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W6_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W6_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W6_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W7_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W7_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W7_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W7_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY length(server_ip) ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W8_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W8_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W8_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W8_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY length(server_ip) DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W9_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W9_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W9_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W9_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W10_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W10_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W10_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W10_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W11_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W11_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W11_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W11_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W12_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W12_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                        timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W12_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W12_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                           timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W13_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W13_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W13_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W13_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W14_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W14_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W14_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W14_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W15_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W15_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W15_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W15_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W16_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W16_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip,
                                                        time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W16_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip, time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W16_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip,
                                                           time_floor(__time, 'PT12H') DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W17_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W17_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip ASC,
                                                        length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                               timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W17_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W17_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip ASC,
                                                           length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                                  timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W18_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W18_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip
                                               ORDER BY server_ip DESC,
                                                        length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W18_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W18_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip
                                                  ORDER BY server_ip DESC,
                                                           length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                   timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W19_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W19_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip ASC,
                                                        length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                               timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W19_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip ASC, length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W19_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip ASC,
                                                           length(server_ip) ASC, cast(length(server_ip) AS DOUBLE)+0.123 ASC,
                                                                                  timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W20_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W20_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (
                                               ORDER BY server_ip DESC,
                                                        length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W20_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (
                                   ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (
                                  ORDER BY server_ip DESC, length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC, timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W20_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (
                                                  ORDER BY server_ip DESC,
                                                           length(server_ip) DESC, cast(length(server_ip) AS DOUBLE)+0.123 DESC,
                                                                                   timezone RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W21_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W21_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W21_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W21_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (PARTITION BY client_ip RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W22_G1_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W22_G1_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM
  (SELECT server_ip,
          client_ip,
          timezone,
          time_floor(__time, 'PT12H') __time,
          count(*)
   FROM kttm_nested
   GROUP BY server_ip,
            client_ip,
            timezone,
            time_floor(__time, 'PT12H'))
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22') WINDOW w AS (RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W22_G2_Q1 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) first_val,
       last_value(timezone) OVER (RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING) last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H');
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# TEST_ID: A8_first_last_W22_G2_Q2 TYPE: NEGATIVE TEST
#-------------------------------------------------------------------------
SELECT server_ip,
       client_ip,
       timezone,
       cast(time_floor(__time, 'PT12H') AS char) t,
       first_value(timezone) OVER w first_val,
       last_value(timezone) OVER w last_val
FROM kttm_nested
WHERE client_ip IN ('107.13.54.103',
                    '99.9.55.22')
GROUP BY server_ip,
         client_ip,
         timezone,
         time_floor(__time, 'PT12H') WINDOW w AS (RANGE BETWEEN 1 FOLLOWING AND UNBOUNDED FOLLOWING);
QueryInterruptedException
!error

#-------------------------------------------------------------------------
# Total query count 705 Positive tests: 1 Negative tests: 704
#-------------------------------------------------------------------------
