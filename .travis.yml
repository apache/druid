language: java

jdk:
  - oraclejdk8

script:
  - mvn test -B -Pparallel-test -Dmaven.fork.count=2
  - mvn clean -Pstrict -pl '!benchmarks' compile test-compile -B

install: true

sudo: false

cache:
  directories:
      - $HOME/.m2

