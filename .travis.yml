language: java

jdk:
  - oraclejdk8

env:
  - MAVEN_OPTS="-Xmx3000m"

install: mvn test -B -Pparallel-test -Dmaven.fork.count=2 && mvn clean -Pstrict compile test-compile -B

sudo: false

cache:
  directories:
      - $HOME/.m2

